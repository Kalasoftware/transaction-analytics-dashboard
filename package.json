{
  "name": "transaction-analytics",
  "version": "1.0.0",
  "description": "Transaction analytics dashboard with Supabase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"Build completed\"",
    "deploy": "pm2 restart transaction-dashboard || pm2 start server.js --name transaction-dashboard"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "nodemon": "^3.1.11"
  }
}
